* OOM(Out Of Merory)
* 在经典的计算机操作系统中，当内存(RAM)不足时,系统会触发OOM Killer强杀进程,或者将不常用的内存页置换到磁盘上(Swap)


* tine-claw  大模型的上下文窗口(Context Window) 逼近了极限限制(比如128K Tokens),  上下文压缩 -> 添加到 context.md -> 重新开启对话，并加载上次的context.md文件